[package]
name = "pdfcmp"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Perceptual bitmap diff for render comparisons; the canonical visual-test comparator"
# Never crates.io: a test comparator, not a product.
# `cargo xtask check` fails if this line goes missing.
publish = false

[dependencies]
tinker-pdf = { workspace = true }

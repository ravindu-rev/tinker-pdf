# Test corpora

External corpora are FETCHED, never committed. `fetch` scripts pin upstream
commits and checksums; CI restores from cache keyed on the pin file.
Licensing per corpus: `docs/plans/14-testing-and-corpora.md`.

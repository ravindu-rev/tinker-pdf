# Status

What is built, what is not, and what the difference means. Updated as phases
land; the plan files say what *should* exist, this says what *does*.

**2786 tests**, `cargo fmt --check` and `clippy -D warnings` clean,
`wasm32-unknown-unknown` builds, the crate graph is enforced, **the four
package manifests are checked against the one workspace version**, and the
fuzz targets and language bindings type-check — on every commit. The fuzz targets
also *run* on every commit now, briefly, over committed seed corpora. **qpdf
runs on every commit too**, over the linearized output, in a job that reports
red rather than green if qpdf is missing (ruling 9: invoked, never linked).
The determinism fingerprints — **fifteen** since gap 31 added one for a
**reflowable book**, on top of gap 30's fixed document, gap 29's comic archive, gap 18a's JPEG 2000 page, gap 10's
mesh shadings, gap 17's JBIG2 scan, gap 09's tiling patterns and gap 11's
transparency groups and soft masks — reproduce byte-for-byte on
`wasm32-wasip1` under wasmtime **and on `x86_64-unknown-linux-gnu`**, both run
locally against native Windows, neither yet observed in CI. That is three of
ruling 4's four targets measured on one machine; macOS is claimed from the CI
matrix and cannot be settled here.
The thirteenth was the first whose document is **synthesised
rather than parsed**, so it pins the writer and the parser to each other as
well as the renderer; the **fourteenth is the first whose input this repository
did not author** — a package Microsoft's own serialiser wrote — which is a
different guarantee again, because a fixture we wrote can only disagree with us
about arithmetic and one we did not can disagree with us about the format. Each
is joined by a hash of a different kind: the synthesised document's own bytes,
which is where object numbering, dictionary key order and stream framing are
pinned, none of which a rendered hash can see.

The **fifteenth is the first whose page number is not a property of the file**.
A book's page count is a function of the box the caller passes, so ruling 4 has
a second half here that no earlier format could ask: the same book at 432 × 648
and at 600 × 800 must each be stable on every target **and the two must
differ** — seven pages against six — because a build that ignored the argument
would satisfy "stable" twice over and be exactly as wrong as one that
paginated at random.

> **It was wrong again.** A second audit in August 2026 checked this file
> against the code in *both* directions and found the gap table accurate but
> incomplete: **22 things listed as built are absent or materially thinner
> than claimed**, seven of them producing silently wrong output or unopenable
> files. They are written down in [audit-2026-08.md](audit-2026-08.md), with
> the fixed ones struck through there. Three more rows have been added since,
> found by the gap work rather than by the audit: an incremental update that
> chained to the wrong offset, and the two form defects PRE-E existed to close.
> A known gap is manageable; a false claim is not, because nobody goes looking.

> **This file was wrong for a long time.** A 47-agent audit against every plan
> file in August 2026 found ~35 of ~211 milestones genuinely complete, ~123
> partial and ~53 missing, and found this file claiming several of the missing
> ones as done. What follows is written to be checkable rather than
> encouraging. Where something is partial, the gap is named.

## What the audit changed

Fixed since, each with tests that would have caught it:

| Was | Now |
| --- | --- |
| **Four of eleven fuzz targets never compiled.** Wrong arity since written; `fuzz/` sits outside the workspace so nothing checked it | Fixed, and `cargo check` on the fuzz crate is a CI job. The bindings, excluded for the same reason, are too |
| **A `cmap` format 12 group could overflow a glyph id into a panic.** `startGlyphID + (code - startCharCode)` in `u32`, all three from the file — the first thing the fuzzers found, by two targets within thirty seconds of each other. Format 12 is the subtable used past the BMP, so any CJK or emoji face reaches it (ruling 1) | `checked_add` before the `u16::try_from` that was already there and could never run. The 68-byte reproducer is a test in `sfnt.rs` and a seed in two corpora |
| **A CFF font drew the wrong glyphs, or none.** The character code was used as the glyph index at three sites — the bare `/FontFile3`, the `CFF ` table of an `OTTO` face, and the TrueType fallback. Which symptom a file got depended on its glyph count: a *subset*, which is what a producer embeds, had no glyph at that index and the page came out blank; a full font drew the wrong letter at the right width, silently. Nothing read the charset, the encoding, the string INDEX or `FDSelect`, so no better answer was available | The charset in all three formats and the three predefined ones, the string INDEX and the 391 standard strings, the built-in encoding with supplements, `ROS`/FDArray/FDSelect with per-FD private dicts and matrices, and 9.6.6's fallback order ending in `.notdef` *and a warning*. Twenty-four tests; the fixtures are boxes of different sizes, so the assertion is which glyph drew, not that one did |
| **A composite font drew the wrong characters at exactly the right spacing.** 9.7.4 makes a Type 0 font's code a byte sequence and the CID it maps to the thing that selects both the advance and the glyph. Only the advance was told: `width_of` looked its number up by CID and the glyph lookup was handed the code, which went through `/ToUnicode` to a character and the character through the font program's own `cmap` — a table that answers "which glyph draws this character", which is not a question a composite font asks. `/CIDToGIDMap` (9.7.4.2) was read nowhere at all, and a subsetter writes it because subsetting renumbers glyphs and the CIDs must not move with them. A CJK page therefore laid out perfectly and read as a font substitution rather than a bug | `DecodedCode` carries the CID beside the code, from the same call the width goes through; `/CIDToGIDMap` is read in both forms, with a CID past the end of the table drawing `.notdef` *and reporting* rather than wrapping onto entry zero; and a composite font selects its glyph by CID. Sixteen tests over a hand-built CIDFontType2 whose code, CID and glyph are three distinct numbers — 0x41, 7 and 3 — and whose face carries a decoy `cmap`, so what proves the CID drove the choice is that the decoy's glyph is *not* what drew |
| **Vertical text advanced by horizontal widths.** A Japanese document set vertically was detected as vertical -- the `-V` suffix and `/WMode 1` both reached `Font` and the pen moved down -- and then moved down by `/W`. 9.7.4.3's `/W2` and `/DW2` were parsed nowhere in the tree, so `width_of` was the only advance on offer and the writing mode chose an axis for it rather than a number. The position vector went with them: it is the other half of 9.7.4.3, it shifts a glyph inside its em box for vertical setting, and it was applied at no point at all, so a comma sat in the horizontal corner of a vertical column | `/W2` in both of Table 117's forms and `/DW2` defaulting to `[880 -1000]`, read beside `/W` where `/CIDToGIDMap` is read and for the same reason (ruling 8); a `vertical_metrics(cid)` accessor returning all three components, because a signature shaped like `width_of` would have invited the same omission twice; 9.4.4's `ty` on the pen, which is a signed downward number rather than a negated advance; and the position vector inside `Trm`, so it scales with the font size. Three defects nothing had named: `TJ` moved a vertical pen *sideways*, so a kerned column drifted out of itself while its spacing never changed (9.4.3 puts the adjustment on the coordinate the writing mode names); `Th` was multiplied into the vertical advance, which 9.4.4's `ty` does not carry; and `Glyph::advance` had no documented sign. Twenty-four tests over fixtures whose `/W` is a decoy at 1000 units against vertical displacements of -400, -600 and -900, on three *non-adjacent* CIDs so a stride error lands in a gap, asserting absolute positions so a sign error changes the number rather than the shape |
| **Every CFF INDEX was read one byte early**, so no embedded CFF or OpenType/CFF face ever resolved a glyph — `Cff::parse` refused almost every real program, and `resources.rs` read that as a font it could not read. The tests covered DICT operands and subroutine bias, none of which builds an INDEX, so nothing had ever parsed a whole program | The 1-based offset is subtracted once rather than twice. Two tests: the eight-byte reproducer, and a whole three-glyph program that also serves as the `cff` fuzz seed |
| **`authenticate` failed on any shared document.** `Arc::get_mut` returns None once a `Page` exists, so "look at a page, then supply the password" returned `NotEncrypted` for an encrypted file | Interior mutability; `authenticate(&self)`, as the plan always specified |
| **`/Rotate` was never applied**, though the canvas *was* sized for it — rotated pages drew upright and clipped | `page_view_transform`, with the crop-box origin as well |
| **`J j M d` were discarded**, so every stroke was solid, butt-capped, miter-joined, while the rasterizer's tested implementation sat unreachable | Wired through the graphics state |
| **Pattern fills painted black.** Every gradient from a design tool became an opaque rectangle | Shading patterns paint; tiling patterns did too, from gap 09 |
| **Pattern *strokes* still painted black, and said nothing.** `gs.stroke_pattern` was recorded by the interpreter and read by nothing, so `stroke_path` and the stroking half of `show_glyph` asked for `stroke_color` — which is `/Pattern`'s nominal black, because 8.7.3 gives a pattern space no colour of its own. A gradient-stroked rule came out solid black with an empty warnings list, and a *tiling* pattern stroked came out black where the same pattern filled correctly warns and paints nothing. That second half falsified this file's own gap row | A stroke is a fill of its outline, so both call sites route the outline `stroke()` already computes into the existing `fill_with_pattern` — no pattern machinery was added, and the warning path is shared rather than duplicated, so the two cannot diverge again. `[/Pattern base]` (8.7.3.2) now parses and an `scn`'s components reach the slot's colour, which they did for neither slot before. Fourteen tests; the two that carry the weight are caught by exactly one assertion each — a stroke under two CTMs asserted byte-identical (8.7.3.1 anchoring), and a self-crossing stroke whose overlap must survive the non-zero rule |
| **Tiling patterns were reported, not painted.** `PatternType 1` was recognised, warned about and left blank — correct, documented, and a hole in every technical drawing and every patterned fill. The degradation was honest, so this was a missing feature rather than a defect; what made it dangerous was the *shape* of the fix, because anchoring a lattice to the paint-time CTM instead of to 8.7.3.1's parent default space reads as a small offset rather than as a bug and would have turned a tested gap into a false capability | The cell is rasterised **once** into a `/BBox`-sized offscreen canvas and composited across the lattice through gap 11's `Canvas::composite`, whose walk is the source's rectangle. Replaying the content stream per position — the obvious build — is correct and costs a page-sized mask clone per cell, several gigabytes for one A4 hatch. Both paint types, `/XStep`/`/YStep` gaps *and* overlaps, the pattern's own `/Resources`, fills, strokes and pattern-stroked text. `PaintType 2` recolours the finished cell rather than intercepting colour at four paint sites, and takes the **stroking** slot's colour on a stroke — data gap 07 stored and nothing had ever rendered. Three bounds, checked before a cell is drawn: 65 536 lattice positions, 16.7 Mpx per buffer, 33.5 Mpx of compositing per fill; past any of them the fill paints nothing and names the pattern. Thirty-three tests, a net twenty-eight, and the ninth fingerprint; injection shows paint-time anchoring is caught by exactly two of them, the fill one and the stroke one |
| **Mesh shadings were reported, not painted.** `ShadingType` 4 to 7 were recognised, warned about by number and left blank — correct and complete degradation, so this was a missing feature rather than a defect. What made the *shape* of the fix dangerous is the same thing that made tiling patterns dangerous: a mesh filled triangle by triangle through the ordinary path anti-aliases every shared edge against the backdrop, which reads as a bug in the blend rather than in the shading and is very hard to attribute. **Built by an explicit decision over the corpus evidence, not because of it** — gap 23 put mesh shadings at 10 files of 4 525, the lowest of the three deferred capabilities, and the argument for building anyway is that none of the three pinned corpora samples design or CAD output, which is where gradient meshes live | All four types: the packed vertex stream with every width Table 79 allows, the edge-flag continuation for triangles and for patches, `/Decode`, `/VerticesPerRow` and `/Function`. A Coons patch is stored as the tensor patch 8.7.4.5.7's formula makes it, so there is one surface evaluator rather than two, and it subdivides at a **fixed** step count — a Manhattan control-polygon length in `f64`, cast once to 16.16 and compared as integers, with the loop doubling its threshold rather than halving the length. The whole mesh goes through **one buffer**: coverage from a single non-zero `fill`, whose signed-area accumulation makes a shared edge whole, and colour from a per-triangle scanline walk, interpolated in the shading's own space and converted per pixel after. Four bounds, one of which is a *total* rather than a per-item cap for the reason `MAX_TILE_WORK` exists. Thirty-seven tests and the eleventh fingerprint; injection put the per-triangle seam at 192 of 255 coverage and found three blind spots, including a winding guard nothing in the workspace could see because a shared edge never needed it — an *overlap* does |
| **`/Function` arrays truncated to the first element**; **Separation tint transforms were the identity**; **`/Lab` was aliased to RGB** | All three read properly; Lab converts through XYZ |
| **`/SMask` was never read** — every soft-masked image painted an opaque rectangle. `/Decode` likewise. Stencils were hardcoded black | All three honoured |
| **Text render modes**: mode 1 filled instead of stroking, modes 4–7 never clipped | Fill, stroke and clip decided independently; clip accumulates to `ET` |
| **Inline images were skipped entirely**, and once they drew they ran a filter chain of their own — no predictor, `/EarlyChange` hardcoded, DCT and CCITT refused. The abbreviation rewrite needed a space after each name, so `/F/Fl` left the whole `/Filter` entry unread | Decoded through `CosDocument::filter_chain`, the same mapping the three stream tiers use, under the same `MAX_DECODED_STREAM` ceiling. Table 93's keys expand name by name, delimiters included. An inline JPEG and an inline fax decode, the fax through the one `ccitt_samples` the XObject path uses |
| **~2330 lines of `src/semantics/` never compiled** — no `mod` declaration anywhere. Anyone grepping concluded phase 04 was done | Deleted. `link.rs` was the only part describing something the live tree lacked, so links are reimplemented and tested |
| **No deflate encoder existed**, so `compress` had no reader and every written byte was uncompressed | Implemented; round-tripped against our own inflate |
| **Object streams produced files that opened with zero pages** — the source trailer's stale `/Size` overwrote the correct one | Fixed. This had been true since the feature landed |
| **Rewriting an authenticated encrypted document emitted `/Encrypt` over plaintext** | `/Encrypt` dropped on rewrite |
| **"Full rewrite" had no garbage collection** | Opt-in mark-and-sweep |
| **Redaction read `Tm` as translation only**, so scaled text cut the wrong glyphs — the worst failure mode for a redaction, because it looks like it worked | The whole matrix is read; rotated runs are refused rather than cut wrongly |
| **`/FontFile` (a Type 1 program) went to the sfnt and CFF parsers**, which declined it correctly, so embedded Type 1 fonts drew nothing and said only that some font was unreadable | Type 1 is read: eexec, charstrings, `seac`, flex |
| **`xtask` was a stub that exited 2**, so nothing checked the crate graph — a design rule the compiler cannot enforce | `cargo xtask dag`, in CI, with the one undeclared edge written down and justified |
| **Type 3 glyphs never drew.** A Type 3 glyph is a content stream, not an outline, and there was no path for one — no warning either, because nothing was missing | The interpreter recurses into the procedure, with the `/FontMatrix` inside the placing transform and the graphics state restored after |
| **An EOL aborted a whole CCITT image.** T.4 separates rows with one and `/K > 0` streams always carry them | EOL and EOFB recognised; a damaged row repeats the row above rather than ending the page |
| **`Document::open` collapsed every failure into `NotAPdf`**, so "needs a password" and "not a PDF" were the same answer | `readable()` reports `PasswordRequired`; `OpenError::Empty` separates zero bytes from bad bytes |
| **`TextPage` had no warnings**, so "no text" and "text this build could not decode" were indistinguishable | `TextPage::warnings`, deduplicated |
| **`deny.toml` could not express the hand-rolled rule** — it lists licences, which say nothing about what a crate *does* | The crates that would violate it are denied by name |
| **Redaction ignored `cm`**, so content under a transform was measured in the wrong space | The matrix is composed, saved and restored with the pen |
| **An embedded font carried its whole face** — tens of megabytes to set a line of Latin text with a CJK font, which is the reason nobody embeds | Subset to the glyphs the document draws, with composite components followed and the 9.6.4 name tag written |
| **Nothing could write a linearized file**, so a viewer could not show page one before the last byte arrived — the capability MuPDF 1.26 removed, and the reason Tinker's plans had to shell out to qpdf | Annex F layout: parameter dictionary first, a cross-reference table for page one ahead of it, page one's objects before everything else, shared objects last, and the final `startxref` pointing back to the front |
| **Form XObjects were not clipped to their `/BBox`** (8.10.2), and forms are how most producers place repeated content — so a form drawing outside its box painted over the rest of the page, on every page it appeared | Clipped, with the corners transformed into device space; the annotation case, which is the same defect one layer up, is fixed too |
| **`pdfcmp` gated on the mean channel difference** while promising its budgets transfer from Tinker's, which counts changed pixels — a glyph moving one pixel barely moves the mean, so the tool would report "within budget" for exactly the regression it exists to catch | Gates on the fraction of pixels that moved by more than a threshold, with Tinker's own constants as the defaults |
| **`cargo xtask dag` never checked xtask's own dependencies** — it looked for `tools/xtask/Cargo.toml`, which does not exist, and a failed read was a silent `continue` | Paths rather than names, and an unreadable manifest is a reported problem |
| **Progressive JPEG was refused, not decoded** — in four of the first eight real files, so their photographs rendered as grey placeholders | Spectral selection, successive approximation for DC and AC, and end-of-band runs. Baseline moved onto the same coefficient buffer rather than keeping a second path |
| **`extend` shifted by an unclamped magnitude category**, so any JPEG with a corrupt Huffman table panicked — reachable from baseline since the decoder was written (ruling 1) | Clamped to the largest category T.81 defines |
| **A single-component JPEG scan was decoded over the MCU-padded block grid** instead of the component's own, desynchronising every later block where the sampling factors are not 1×1 | Non-interleaved scans iterate their own dimensions (T.81 A.2.2) |
| **Platform `libm` was called on three pixel paths** — round joins, the PostScript calculator, the sRGB transfer function — so bit-identical cross-target output was impossible whatever CI measured (ruling 4) | `tinker-pdf-math`, built only from operations IEEE 754 pins exactly, plus `cargo xtask libm` to keep it that way |
| **`Document::cos` returned a type the facade did not export**, and `DocumentBuilder` was not exported at all, so a caller depending on the facade alone could neither use the escape hatch nor write a file | Both exported |
| **Redaction stopped at the page stream.** Forms are how most producers place repeated content, so a redaction could be driven straight through one; images under a rectangle were covered, not removed | Forms are rewritten recursively; images are scrubbed to a blank sample |
| **Page operations were silently dropped by a rewrite** — the reordered `/Kids` was written into the incremental set only | Applied to whichever set the mode builds. Every existing page-operation test saved incrementally, which is why nothing caught it |
| **`WriteOptions::encryption` had no reader**, so asking for encryption produced a plaintext file with no error | R6 encrypt-on-save, strings and streams, per-object IVs |
| **Asking for encryption *and* linearization gave an encrypted file with an ordinary layout**, no error and no warning — `rewrite` guarded the linearized path with `encryption.is_none()`, so encryption won and the layout request was discarded | Both, together. Objects are serialised and encrypted in one pass and the layout is measured from the ciphertext, which is what keeps the writer's no-patching property: measure first and every offset after the first stream is short by the accumulated padding, which this engine's reader recovers from and `qpdf` does not — `file is damaged`, `File is not linearized` |
| **Every linearized file's hint tables were nonsense**, and nothing could tell. They are bit-packed structures this repository wrote and nothing here read, so five separate faults survived with the whole suite green: Table F.6's one-bit signature flag written at *zero* width — `overflow reading bit stream: wanted = 1; available = 0`, and the `1` is that flag; entries packed by row where every reader unpacks them by column, each run padded to a byte boundary; Table F.3 item 2 holding the first page's object *number* where its byte *offset* belongs; each page's objects not numbered as the consecutive run led by its page object that F.4.1 makes a reader take, so three numbers from `/O` ran off the end of the file; and `/T` and `/E` pointing at the `xref` keyword and the end of the hint stream rather than at the main table's first entry and the end of part 6 (Table F.1) | All five, plus the rule none of the error messages named: an offset inside a hint table is measured as though the primary hint stream were not in the file, so everything behind it is short by that length. The hint stream moved to part 5 where F.3.1 puts it, ahead of the first page's objects rather than behind them. `qpdf --check` and `--show-linearization` run in CI over four fixtures, encrypted and not, in a job that goes **red** if qpdf is missing — measured by removing it from `PATH`, where `cargo test` exits 0 and reports `2 passed`. A round-trip reader beside it checks the tables offline; injection puts it at nine faults out of ten, and the tenth is why the oracle is not optional |
| **`/EncryptMetadata` and `/Crypt /Identity` were ignored**, so exempt streams were decrypted into noise | Both honoured |
| **Comb fields laid out as ordinary text**, drifting out of their printed cells | Laid out in cells |
| **Attachments, XMP and `/Limits` descent were unreachable** | All three implemented |
| **The font seam was Rust-only**, so no binding could draw text for a document embedding no fonts | `set_fonts` across the C ABI, Python, JS and .NET |
| **`/Info` collapsed blank onto absent.** A field present and empty — or all whitespace — read as `None`, identically to a key the document never wrote, so no caller could tell "the producer left the title blank" from "there is no title" | Present and a string is `Some`, whatever it holds, untrimmed. Plan 04 always called this a contract; the code and the comment on the line both said the opposite |
| **A stale catalog `/Version` demoted the file it sat in.** 7.7.2 lets the catalog raise the header's version, because that is how an incremental update declares a later one without rewriting header bytes a signature covers; the code let it *set* the version, so a 1.7 file carrying `/Version /1.4` reported 1.4. A repaired file whose header was unreadable reported no version at all rather than the baseline plan 04 specifies, and `/Trapped` — the ninth `/Info` field, and the one a print workflow needs — was not read | The later of the two wins, compared as the `M.N` number pair so `1.10` outranks `1.9`, with an unparseable version on either side treated as absent rather than as zero. No readable version on either side reports the 1.7 baseline and emits `HeaderMissing`, so the guess is on the record. `/Trapped` reads as its three names, with an unrecognised one as `Unknown` and only an absent key as `None` |
| **An embedded CMap could not inherit.** `usecmap` (9.7.5.3) is how a CMap says "everything the parent defines, plus these changes", and it appeared nowhere in the engine; the CMap stream dictionary's `/UseCMap` was not read either, because the dictionary's keys were never looked at. A differential CMap — the normal shape in Japanese production workflows — therefore got only the changes, and since the parent is what supplies the codespace ranges, the child had none: `decode_codes` fell back to one byte per code, so every two-byte code became two fragments and each fragment selected a CID the file never named. Alongside it, the `end*` operators were not matched by name. Each section loop stopped at the first token of an unexpected *shape*, so a truncated section ended as quietly as a complete one **and consumed the keyword that ended it**, losing whatever the next `begin*` opened | Both spellings resolve and merge: the child's ranges override the parent's where they overlap, the parent's survive where they do not, and the child's `/WMode` wins where it declares one. The chain is capped at four links and refuses a source already on it, so a CMap that uses itself terminates. Ruling 8 kept the fetch out of the leaf crate — the chain is walked in `tinker-pdf-font` and the parent is fetched through a resolver closure from `tinker-pdf-cos`, the shape `/DecodeParms` already uses. Sections now close on their own operator, with a typed warning naming which section and which stream (ruling 10). Thirty-three tests over hand-built parents that answer the same codes with *different* CIDs, asserted in parsed ranges, in a document, and in pixels — with the merge stubbed out, sixteen of them fail |
| **Every predefined CMap but `Identity-H`/`-V` was a stub.** 9.7.5.2 names Adobe's published CMaps and defines none of them, so the registry is the normative statement of what `90ms-RKSJ-H` means -- and it was not in the repository. `CMap::predefined` matched a name against fourteen registry prefixes and answered a match with one blanket two-byte codespace and `identity: true`. Both halves were wrong. The codespace mis-split every mixed-width encoding -- RKSJ, GBK, Big5 and the EUC families all have one-byte *and* two-byte ranges -- so a string had the wrong number of codes in it before any mapping happened, and because the CID drives `/W` the advances went with the glyphs. The prefix list was also wrong in both directions: `RKSJ` matched nothing, because no registry name *begins* with it, while `83pv-RKSJ-H`, `78-RKSJ-H`, `Hankaku`, `Roman`, `HKscs-B5-H` and the whole `UniHojo-*` family fell through to `None` and were then split one byte per code. Nothing reported any of it: `is_approximate` existed, was public, and had no caller anywhere | All 202 CMaps of Adobe's registry are vendored (`THIRDPARTY.md`) and compiled by the workspace's first `build.rs` into 1.19 MB of delta-encoded, deflated tables -- with the engine's own encoder, so ruling 3 holds. The registry's own `usecmap` is followed, so `90ms-RKSJ-V` is 78 ranges over `90ms-RKSJ-H` rather than a copy. Codespaces are matched **per byte** as 9.7.6.2 says rather than as one integer interval, which is what separates GB18030's two-byte and four-byte codes, and an undefined code takes its length from its lead byte as 9.7.6.3 says rather than consuming one byte and re-reading a trail byte as a lead. A name the registry does not define is refused and *named* in a warning, in both spellings, rather than guessed. Behind `cmap-predefined`, default on; with it off the codespaces still ship in 4.6 KB, so strings still split correctly, and `is_approximate` reports that the CIDs are missing. Thirty-three tests, of which the two that carry the weight check 805 632 range bounds and 474 codespace bounds against Adobe's own text through a second, deliberately different reader -- because a round trip through our own encoder proves only that the encoder agrees with itself |
| **The `text` determinism fixture rendered nothing.** It named Helvetica and embedded no program; the engine bundles no faces, so every glyph resolved to nothing and its committed fingerprint was — bit for bit — the hash of a blank 200×100 page. The only pixel baseline in the repository covered no glyph at all, and five gap plans were queued behind "the fingerprints did not move" | The fixture embeds a synthetic TrueType face of curves, diagonals and a hole, built in the test file. Every fixture now asserts a minimum ink count and the absence of `UnreadableFont` before it is hashed, so a fixture that draws nothing fails instead of becoming the baseline |
| **Optional content did not exist.** Clause 8.11 was absent from `crates/` entirely -- `OCProperties`, `OCGs`, `OCMD`, `/OC` and the `/Properties` resource sub-dictionary appeared only in plan text. `BMC`, `BDC`, `EMC`, `MP`, `DP`, `BX` and `EX` fell into the interpreter's catch-all, so a layer a file marks `/OFF` -- a CAD drawing's construction lines, a map's alternate language, a proof's annotation overlay -- was painted at full opacity over whatever was underneath, with an empty warnings list. `/OC` on a form or image XObject was read nowhere either, so a layer carried as one XObject was drawn whole | The catalog's `/D` configuration with `/BaseState`, `/ON` and `/OFF`; OCMD with all four `/P` policies and `/VE` visibility expressions; 14.6.2's nesting, with an unbalanced `EMC` dropped rather than reaching out of the stream that wrote it; and `/OC` on both kinds of XObject, expressed as a marked-content scope around the `Do` so that a hidden image skips its *decode* rather than reporting an unsupported codec for something nobody was going to see. Suppression is at the **paint**, in the `Device`: hidden content still advances the text pen, still balances `q`/`Q`, still installs a clip, and still extracts as text, so the two devices behind ruling 7's one interpreter cannot disagree about what a page contains. Everything not understood renders -- a malformed `/VE`, an unrecognised `/P`, a group the catalog never listed, a cycle -- because content wrongly hidden is worse than content wrongly shown: the reader cannot tell it is missing. `HiddenOptionalContent` names the layer (ruling 10). Forty-three tests; the policy table is twelve rows because `/AnyOff` agrees with its own inversion on four of the six arrangements it touches |
| **Every TrueType quadratic was raised to a cubic before it was flattened**, on the hottest path in the engine, for a control point the curve does not have. Plan 07 specifies a `QuadTo` verb and the path model had none. The up-conversion needed the current point and asked the path for it, which cannot answer after a `Close`: it fell back to the curve's *own* endpoint, collapsing both control points onto one place and drawing a visibly different curve — 138 of 1600 pixels wrong on the fixture that reproduces it. No `glyf` outline reaches that case, because every contour there opens with a `MoveTo`; `GlyphSource` is public, so any host can | `Verb::QuadTo` is first class, `show_glyph` emits it, and the up-conversion and its helper are deleted — the verb carries no start point, so there is nothing left to look up wrongly. The quadratic arm of the flattener takes its step count from the cubic the curve is equal to, so the same curve breaks into the same pieces whichever verb describes it; measuring the quadratic's own control legs instead would have given one typeface two smoothness characters wherever it mixes `glyf` and `CFF` outlines. Moves the `text` fingerprint by 7 of 20 000 pixels, one level each, ink count and bounding box unchanged, with wasm32 and x86_64 agreeing on the new value before the table was touched. Plan 07 is amended in place twice: the verb set now matches it, `f32` points still do not, and its "adaptive recursive subdivision" describes a flattener the crate has never had — a float termination test is what ruling 4 keeps off a pixel path |
| **Every image was sampled with one truncating tap per destination pixel**, and the sampler was in the wrong crate. `tinker-pdf-raster` exported `blend`, `canvas`, `fill`, `geom` and `stroke` and had no image entry point at all, so twenty lines of loop in `Renderer::blit` were the whole of it — nowhere to put a filter, and nothing testable without building a page. A photograph in a thumbnail kept whichever source pixels the sampling grid happened to hit; a logo magnified had hard stair-steps. `/Interpolate` (Table 89), which is how a file asks for the other behaviour, was in the inline-image abbreviation table and read by nothing after that: no field on the decoded image, no branch at the sample site | `tinker-pdf-raster::image`, taking bytes, dimensions and a transform — no COS type crossed the boundary (ruling 8), and 8.9.6.2's stencil left its PDF name behind as a `tint`. Plan 07's four rows, all of them: nearest at or above 1:1 without the flag, bilinear with it, bilinear on the way down, and a box pyramid to within 2:1 beyond that, per axis, with the levels handed back so phase 08 owns the cache. Measured rather than eyeballed — a magnified gradient's largest step 32 to 2, a 2:1 checkerboard from one phase of the board to a uniform 128, a 16:1 one to mean 128 at variance 0. The level count branches on 16.16 integers because `log2` is a transcendental and a count one different is a different image. **The seventh fingerprint**, whose first draft was blind to the defect it existed for: a linear ramp and a checkerboard are both reproduced identically at any pyramid depth |
| **Every paint cost a whole page.** All four `fill()` call sites in `tinker-pdf-render` asked for a mask the size of the canvas, so a comma at 300 dpi allocated and rasterised 8.4 MB, and so did every clip and every fill; `Path::bounds` existed with no caller outside its own unit test. Two more layers underneath were O(canvas) as well: `Canvas::fill_mask_with` walked the whole canvas whatever the mask covered, `Mask::intersect` allocated a second page-sized mask, `fill_with_pattern` ran its inverse transform and its shading function over every pixel, and `fill`'s row loop scanned every row testing every edge at all sixteen sub-scanlines — `height x 16 x edges`, so a 500-edge signature cost the same on a 200 px page and on an 8000 px one | A region per path, widened by one pixel of measured slack, clipped to the canvas and to the clip's own rectangle, decided in the one place every paint and every clip goes through; consumers that walk the mask's rectangle; and an active-edge list over the rows a shape actually reaches. **600 glyphs on US Letter at 300 dpi: 5997.7 ms to 18.8 ms**, 318 times, and a 500-edge path over a 16x taller page went from 8.3x the cost to 0.99x. Output is bit-identical by construction and the six fingerprints did not move — which is why the durable part is `a_small_fill_on_a_large_page_stays_small`, an assertion on mask pixels asked for rather than on pixels painted: a paint that goes back to the whole canvas fails it, and passes everything else in the repository |
| **Every incremental update chained to the wrong offset.** 7.5.5 Table 15 makes a trailer's `/Prev` the byte offset of the previous cross-reference *section*; `CosDocument::last_startxref` — whose own doc comment said exactly that, and whose only caller is `DocumentEditor::save` — returned the byte just past the last `%%EOF`, which is never where a section starts. So every incremental update this engine had ever written published a chain no reader could follow, and **every object the update did not itself carry was unreachable behind it**: a form filled through `set_field_value` reopened with its `/V` present and its widget annotations, its appearance streams and its `/DR` font all reading as null | The section's own offset, recorded on `Revision` as `xref_at` and corrected for any header shift (7.5.2), with `/Prev` omitted altogether when there is no section to name rather than written as `/Prev 0`. Nothing had caught it because the reader recovers, and only sometimes: a file whose catalog was *not* in the update falls to ladder level 3, rescans the whole buffer, finds every object by its header and reads correctly — which is the case every page-operation test in the repository happens to exercise — while a file whose catalog *was* in the update stays at level 1 and quietly reads the rest as null. The regression test therefore asserts the **ladder level and an empty warnings list**, not the content, because the content is right either way in the case that was already covered |
| **A form field on two pages could be half filled and report success**, and nothing could express "these fields apply together or not at all". The widget loop skipped a widget with no `/Rect` and `set_field_value` returned `true` regardless; measured on exactly that shape, one widget drew `(Ada) Tj`, the other still drew `(Bob) Tj`, and `/V` said `Ada`. There was no begin, commit or rollback anywhere on `DocumentEditor`, so a refused second value left the first one written — `name` read back `Ada` and `short` read back nothing, from one call pair | `transaction(\|tx\| ...)`, a closure rather than a triple because the failure being prevented is *silent* and a closure has no scope exit that neither commits nor rolls back. It snapshots all four of the editor's mutable fields — overlay, deletions, page order and the object-number counter — and restores them on `Err`; the counter is restored deliberately, because leaving it advanced grows the file on every **failed** edit, and the price, a reused object number, is written where the field is declared. 12.5.2 makes `/Rect` required, so a widget without one is damage rather than a widget with nothing to draw: ruling 2 degrades, so `fill_field` writes the value and draws what it can, and ruling 10 applies, so it returns typed `SkippedWidget`s naming the object. `set_field_value` keeps its `bool` and now means it. The happy path is pinned byte-for-byte against the previous implementation; injection puts each of the four restores under a test that is the **only** one to catch it |

## Built

| Phase | State | What works |
| --- | --- | --- |
| [01 COS](plans/01-cos-and-object-model.md) | milestones 1–4 | Lexer, object model, xref in every flavour, object streams, lazy `Send + Sync` store, repair scanner, leniency ladder, three stream tiers |
| [02 Filters](plans/02-filters.md) | wave 1 + deflate + JPEG + CCITT + **JBIG2 generic region** | Own inflate and deflate, LZW, ASCIIHex/85, RunLength, predictors; JPEG **baseline, extended sequential and progressive**; CCITT G3/G4; **JBIG2's generic-region lineage — the MQ arithmetic coder of T.88 Annex E in its own module, segment headers, `/JBIG2Globals`, templates 0–3 with AT pixels and typical prediction, and MMR through the same T.6 decoder a fax uses.** Symbol dictionaries and text regions are *refused by name*, not skipped: a page that composited no region returns the capability error so the placeholder stands |
| [03 Encryption](plans/03-encryption.md) | reading and **writing** | Own MD5, RC4, SHA-2, AES-CBC; handlers R2–R6; owner vs user distinguished; `/P` read through its reserved bits; `/EncryptMetadata` and `/Crypt /Identity` honoured |
| [04 Document semantics](plans/04-document-semantics.md) | complete | Metadata incl. **`/Trapped`**, the **later** of header and catalog version, page tree with inheritance, geometry, outlines, name/number trees with **`/Limits` descent**, destination enum, page labels, actions, links, **attachments**, **XMP** |
| [05 Fonts](plans/05-fonts.md) | TrueType + CFF + **Type 1** + host seam | Encodings, CMaps, standard-14 metrics; TrueType `glyf`, CFF Type 2 **and Type 1** outlines; **CFF glyph selection: charset, string INDEX, standard strings, built-in encoding, and CID-keyed `ROS`/FDArray/FDSelect**; **composite fonts select by CID, through `/CIDToGIDMap` for a CIDFontType2 and the charset for a CIDFontType0**; **CMap inheritance through `usecmap` and `/UseCMap`, merged so the child wins where it overlaps**; **all 202 predefined CMaps of Adobe's registry, compiled at build time behind `cmap-predefined`, with per-byte codespace matching and a named warning for anything outside the set**; **vertical metrics, `/W2` in both of Table 117's forms and `/DW2` with its `[880 -1000]` default, applied to the pen by 9.4.4's `ty` and to the glyph's placement by the position vector**; `FontProvider` for faces a document does not embed. The font lane is closed — what remains in fonts is shaping, which is a permanent non-goal |
| [06 Content & text](plans/06-content-and-text.md) | substantially | Tokenizer, text state machine, `Device` seam, text device with quads and search, **inline images**, **all stroke parameters** |
| [07 Rasterizer](plans/07-rasterizer.md) | complete | Paths with **first-class quadratics** (`MoveTo`/`LineTo`/`QuadTo`/`CurveTo`/`Close`, nothing up-converted), deterministic anti-aliased fill, stroking with caps/joins/dashes, clipping, compositing, and **image sampling to the plan's four-row policy** — nearest, bilinear and a box pyramid beyond 2:1, in this crate rather than in the bridge above it. Points are `f64`, where the plan says `f32`; the plan records the divergence. **A paint costs what it covers**: a region per shape clipped to the clip's rectangle, consumers walking the mask's rectangle, and an active-edge list over the rows a shape reaches. A long fill or a fine dash expansion **stops when it is cancelled** rather than running to completion, keeping the rows it had already swept |
| [08 Rendering device](plans/08-rendering-device.md) | broad, see gaps | Colour spaces incl. **Lab, Separation, DeviceN**; all four function types **and function arrays**; clipping incl. **text clip modes**; images with **`/SMask` and `/Decode`**; axial and radial shadings; **shading patterns**; **tiling patterns, both paint types, anchored to 8.7.3.1's parent default space, with the cell rasterised once and blitted across a bounded lattice**; **optional content (8.11) from `/OCProperties` `/D`, including OCMD policies and `/VE`**; **`/Rotate` and `/CropBox`**; alpha; outward pixel rounding; page-area ceiling |
| [09 Writing](plans/09-writing.md) | rewrite + incremental + object streams + encryption + **linearization, including both together** | Full rewrite with optional GC, incremental update with byte-identical prefix, classic xref, working object streams, compression, R6 encrypt-on-save, **Annex F layout — and the two combine**: objects are encrypted before they are measured, so the layout describes the ciphertext rather than the plaintext, and the three things a reader needs before it can decrypt stay in the clear. **The hint tables are read back by qpdf in CI**, which is what plan 09's milestone 5 asks for and what nothing did until gap 20; the corpus half of that criterion waits on gap 23. No `/ID` is written on an encrypted save, which 7.5.5 Table 15 requires |
| [10 Editing](plans/10-editing.md) | substantially complete | Copy-on-write editor; delete/move/rotate/**insert/import/keep**; annotations with synthesized appearances **and flattening**; redaction through **forms and images** |
| [11 Forms](plans/11-forms.md) | read, fill, appearance regeneration, **transactional**, **calculating** | AcroForm field tree, fill text/choice/checkbox/radio, **comb fields**, appearances rebuilt, reset. **A fill applies wholly or not at all**: `transaction` restores the editor's whole mutable state on an error return, `set_field_values` is the all-or-nothing multi-field apply built on it, and a widget whose 12.5.2 `/Rect` is missing is *named* rather than skipped in silence. **`/AA` calculate actions run**, on an explicit `recalculate()`: a hand-rolled ECMAScript subset over `/CO` order, applied wholly or not at all. The open JavaScript question is closed |
| [12 Creation](plans/12-creation.md) | pages, text, images, embedded fonts **with subsetting** | `DocumentBuilder` |
| [13 Bindings](plans/13-bindings.md) | four build, are checked, **and now package** — but nothing is published | C ABI, Python, JS/wasm, .NET, each able to **supply fonts**. Gap 26 built the pipeline and ran it **as a dry run, end to end**: one `cp39-abi3` wheel per platform, an npm package (ESM only, `web` target, decision recorded), a NuGet package with `runtimes/<rid>/native/`, and a browser demo that renders an uploaded PDF. On this machine — Windows, x86_64 — all four were built, installed and rendered through, each producing the same 1190 inked pixels. Every Linux and macOS leg, and "one tag produces all four", exist only in `release.yml` and have never run |
| **CBZ** (29) | **built** — the first of the three formats gap 28 decided | A `.cbz` opens as a `Document`: `Document::open` sniffs `PK\x03\x04` **at offset zero and nowhere else**, and the archive is turned into a real PDF at open, so `cos()` still returns a borrow, every existing capability arrives for nothing, and there is no CBZ-shaped branch anywhere below the facade. Ours all the way down: a ZIP reader in a seventh leaf crate (both routes — the central directory and a local-header scan — CRC-32 checked on every entry), a PNG decoder in `filters` (every legal colour-type/depth pair, `tRNS` in all three forms, Adam7, checked against PngSuite's 176 files), and CRC-32, which existed nowhere in this tree. **Pass-through is the design**: a non-interlaced PNG's IDAT reaches the page as `/FlateDecode` with `/Predictor 15` and a JPEG reaches it verbatim, so a 200-page archive costs a multiple of itself rather than *w × h × 3* a page. Pages are in **natural order**, at the image's own pixel size. Two levels of refusal, both by name: an archive that is a CBR, damaged, entirely encrypted, multi-disk, past a bound or holding no images at all is refused at `open`; a single unusable entry becomes a **placeholder page that keeps its number**, because dropping it would renumber every page after it. What is not read: CBR/CB7/CBT, encrypted entries, `ComicInfo.xml`, and any image that is not JPEG or PNG. **No `.cbz` written by a real archiver has ever been opened by it** — every archive in the tree is hand-built from APPNOTE's field layouts |
| [14 Testing](plans/14-testing-and-corpora.md) | tools real, fuzzing **run** | `tpdf`, `pdfcmp`, `oracle-diff`; **24** fuzz targets with committed seed corpora, a nightly job and a short per-PR run; a hostile-input sweep on stable. Gap 24 milestone 5 gave **every one of them** a real session rather than a smoke run — **186 159 981** recorded executions, two crashes, one of them a real lexer defect. Six of the corpora are written by an `--ignored` test in the crate that owns the fixtures, so the seeds and the fixtures cannot drift; `crypt` joined that set after the campaign found its hand-laid layout was one rearrangement away from meaning nothing |

## Not built

Every row below has a plan of its own in [plans/gaps/](plans/gaps/README.md),
linked in the last column — what to build, what to reuse, and how you know
when it is done. [16 Build sequence](plans/16-build-sequence.md) orders them
by value over risk.

| Gap | Consequence | Where |
| --- | --- | --- |
| ~~**No corpus has been run**~~ **A corpus has now been run** | 4 525 documents from pdf.js, veraPDF, qpdf's qtest and the PDF Association, one child process each: **4 484 opened and rendered every page (99.1 %), and not one crashed.** Ruling 1 held against thousands of files nobody here authored — the strongest evidence this repository has for it, and worth more than the fuzzers because these are documents real producers emitted rather than mutations. 40 failed and 1 timed out. The second axis is 1 092 files (24 %) that rendered *with something reported*, and that number is measured **without font faces**: this build bundles none, so it is dominated by documents that embed no font and therefore draw no text. It is a fact about the font policy as much as about the engine, `ratchet.json` says so in its own note, and `corpus-run` refuses to compare a `--fonts` run against a bar recorded without them. What is still missing is the *oracle* half — this measures crashing, not correctness, and `oracle-diff` is a separate question | 23 |
| ~~**Fuzzers run; two targets have had a real session, eighteen have not**~~ **Every target has had a session** | Gap 24 milestone 5 ran the lot: **twenty-two targets**, each with a committed seed corpus, each wired into a bounded nightly job and a short per-PR replay, and every one of them given a real session rather than a thirty-second smoke run — **186 159 981** recorded executions in all, on one machine, in WSL2, because libFuzzer does not exist on `x86_64-pc-windows-msvc`. `png` **122 293 604** executions in twenty minutes and `zip_archive` **4 249 823** (gap 29); `xml` and `render_page` (gap 30, and `render_page` peaked at 1 911 MB against a 2 048 MB ceiling with a 16-second slowest unit against a 25-second timeout, both closer to the edge than is comfortable); the other seventeen at 300 seconds each. **Two crashes.** `cos_object` panicked in 1 569 219 runs on `0.0002E-7700000000000000`, where two `saturating` operations that are each correct compose into exactly `i32::MIN` — no `i32` negation exists for it, so it panicked in debug and **wrapped silently in release**, reachable from any content stream; fixed at `a6a4fef` with both inputs committed as seeds. `xml` crashed on its own assertion and the **crate was right** — a second `U+FEFF` is ZWNBSP and `TextBeforeRoot` is the correct refusal. **`crypt`'s clean result is not filed as a pass**: 2 635 executions in 301 seconds is 8 a second against `ascii_filters`' 61 795, so a session buys it what four *seconds* buys the filters. The hardened hash of 7.6.4.3.3 is slow by design and is only a third of the reason; the rest was the harness carving every `/Encrypt` field at exactly its legal width — a **three-byte** input still built a 48-byte `/O` and `/U` and cost 184 ms — and authenticating twice per input. And nothing past authentication had ever run at all, since `authenticate` returns `Some` only past a 128-bit equality: **`FileKey::object_key`, 7.6.2 Algorithm 1, which every PDF encrypted before 2008 uses on every string and stream, measured 0.00 %**. It is now 100 %, `handler.rs` went 68.73 % to 91.64 % of regions over the seeds, and the four ciphers moved into a `crypt_ciphers` target that runs them at four thousand a second instead of eight. Ruling 1 is measured rather than claimed, and measured *unevenly* — which is the point of recording each target's number beside its result. | 24 |
| **Linearization: proved by qpdf, on fixtures rather than a corpus** | The layout is written, every offset it declares is checked against the bytes, and **the hint tables are now proven by an external reader** — which they were not, and could not have been, because nothing in this repository read them. Gap 20 stood up `qpdf --check` and `qpdf --show-linearization`, the two arbiters plan 09 names, and found the tables wrong five ways: Table F.6's one-bit signature flag written at zero width, which is what `overflow reading bit stream` was; entries packed by row where a reader unpacks them by column; Table F.3 item 2 holding an object number where a byte offset belongs; each page's objects not being the consecutive run led by its page object that F.4.1 makes a reader take; and `/T` and `/E` naming the `xref` keyword and the end of the hint stream instead of the main table's first entry and the end of part 6. Both commands are now clean and warning-free on one-page, two-page, six-page and shared-resource fixtures, encrypted and not, in a CI job that goes **red** rather than green when qpdf is absent. A round-trip reader in `linearize.rs` checks the same tables offline against what the writer computed; injection shows it catches nine of ten faults and that F.4's hint-stream offset adjustment is caught by qpdf alone. **What is still owed is a corpus** — plan 09 asks for the oracle across a linearized corpus subset and there is no corpus here until gap 23 — and an *incremental* update still cannot encrypt, since it would need the original file's key. **No encrypted file carries an `/ID`**, which 7.5.5 Table 15 requires whenever `/Encrypt` is present; R6 does not use it for key derivation so nothing fails to decrypt and qpdf reads, authenticates and fully decrypts the file, but it warns on every encrypted file this engine writes, linearized or not. | 19, 20 |
| ~~**CCITT `/EndOfLine`, `/EndOfBlock` parameters**~~ **FIXED** | All six of Table 11's entries are now read, including `/Rows`, which had never been consulted at all — the image's own `/Height` was passed unconditionally, so a file whose two heights disagreed decoded at the wrong one. `/K > 0` is true T.4 mixed mode: the tag bit sits where 4.2.1.3.1 puts it rather than at the head of every row, so a fax whose rows are not EOL-separated no longer decodes to a blank page. Output is packed 1-bpp and goes through the same sample loop as every other image, so `/ImageMask`, `/Decode` and `/ColorSpace` reach a fax — a fax used to be the one kind of image that could not be a stencil, which is the kind of image a fax most often is. | 16 |
| **JBIG2 *in part*** | Reported with a warning rather than half-decoded. Since gap 07 this is true of *strokes* and *text* as well as fills: it was not before, and the row said it was. A tiling pattern used as a stroking colour painted solid black with an empty warnings list, so the one thing this row promises — that a gap is visible as a gap — was the thing that did not hold. **Tiling patterns left this row in gap 09** and paint; what is still reported here is a pattern whose cell cannot be read at all, and a lattice past the cell budget. **JBIG2 half-left it in gap 17**, and the split is the whole of what this row now says about it: the *generic-region* lineage decodes — the MQ arithmetic coder, all four templates, AT pixels, typical prediction and MMR — and the *symbol dictionary plus text region* lineage does not, and is refused by name. That is the split a reader has to know, because the second is the common case: it is what `jbig2enc` and OCRmyPDF emit and therefore what most JBIG2 that has been through an OCR pipeline is. A file with a region this build cannot decode still draws the placeholder rather than the blank page it was composited onto, which is the whole degradation contract for the codec and is the thing gap 17 spent most of its care on. **Mesh shadings left this row in gap 10** and paint, all four types; what is reported here now is a mesh that cannot be *read* — illegal packed widths, no stream, or geometry past the caps. **JPX left this row in gap 18a** and paints: T.800's container, tier-2, tier-1, dequantisation, both inverse wavelets and Annex G's colour pipeline, reaching a page through 8.9.5.4. It was built **by decision, against gap 18's own amended recommendation and against the corpus**, which puts JPX at 19 files of 4 525 — the argument being that none of the three pinned corpora samples geospatial imagery, medical export or preservation masters. What is reported here now is that plan's enumerated refusal list, every entry of which is reachable and named: RGN, POC, PPM, PPT, CRG, five of Table A.19's six code-block styles, any Part 2 marker, tile-parts out of order, an unmappable `colr`, precision above 16 bits, channels of differing bit depth, and three totals. Measured on the nineteen: **14 decode, 4 refuse by name, and 1 the renderer never asks the decoder for** — that last because a form XObject's own `/Resources` are consulted nowhere in this engine, which is gap 11's recorded non-goal and not this codec's. **It is a claim about nineteen files.** | 17, 18, 18a |
| ~~**Transparency groups, soft-mask groups, blend modes**~~ **FIXED** | `/Group` on a form XObject is read and composited as a unit, with isolation (11.4.4), knockout (11.4.5) and backdrop removal (11.4.7.2); ExtGState `/SMask` is read in both kinds with `/BC` and `/TR`. Both of the failures here render *plausibly*, which is why each is pinned as a number against a control render differing in one key: a group faded element by element shows a seam where two shapes cross, and a backdrop counted twice makes every group in the document slightly more muted. `/BC` absent is **black**, which means fully masked — the opposite default inverts every drop shadow in a corpus and still looks reasonable on a light page. The mask group is rendered at the `gs`, at the transform in force there (11.6.5.2), pinned by two documents differing only in whether `cm` comes before or after it. Group colour spaces are still not honoured: the engine composites in RGB throughout, and a page-level `/Group` is not read. | 11 |
| **Determinism: three of four targets measured, macOS claimed** | ~~Written, unverified.~~ ~~It has now been executed on wasm.~~ **Linux too.** The fifteen fingerprints reproduce byte-for-byte on `wasm32-wasip1` under wasmtime 47.0.3 *and* on `x86_64-unknown-linux-gnu` under WSL2 on `stable`, both against the committed table, which is `x86_64-pc-windows-msvc` — along with the page dimensions and ink counts they are computed from, and the two byte hashes of gap 29's and gap 30's synthesised documents. The linux leg went further than the determinism file: **`cargo test --workspace` there is 2 243 passed, 0 failed, 10 ignored**, the Windows count to the test, which is the first time the whole engine has been run on a second operating system outside CI. What the wasm pairing tests is width — 64-bit against 32-bit, where a `usize` assumption would show — and what linux tests is everything below the arithmetic: a different `std`, allocator, `libm` and linker. Neither caught anything, which is the good outcome, and the plan's own list of suspects has now been checked three times. **macOS is not achievable on this machine** — no Apple hardware, no VM, and Darwin targets cannot be cross-*run* — so it stays a claim from `ci.yml`'s `macos-14` leg, and what would settle it is one observed CI run with that leg green beside a green `wasm-determinism` on the same commit. That is also the outstanding half of milestone 1: the job is guarded against reporting success without running anything, and nobody has watched it run *there*. **Milestone 4 is closed.** It asked gaps 09, 10, 11 and 12 each to add a fixture as it landed and all four did — `tiling`, `mesh`, `transparency` and `image` — and six more arrived from plans it does not name, each for the same reason stated the same way: gap 06's `optional`, gap 07's `pattern`, gap 17's `jbig2`, gap 18a's `jpx`, gap 29's `cbz` and gap 30's `xps`. Fourteen fingerprints against the four this plan opened with, every one carrying an ink floor so a fixture that draws nothing fails on the day it is added rather than becoming a baseline. | [25](plans/gaps/25-wasm-determinism-leg.md) |
| **Binding packaging: the pipeline exists and has been dry-run; still nothing published** | ~~Nothing published; no wheel or per-RID CI.~~ There is now a wheel, an npm package, a per-RID NuGet package and a browser demo, and `cargo run -p xtask -- release` walks all four in an order **computed from the manifests** rather than maintained by hand. The dry run is the default; `--execute` is the flag that publishes, because a half-published release cannot be retracted from crates.io. **Nothing has been published to any registry**, deliberately — the facade does not freeze until 0.1.0 and a published package invites dependence on an API that is explicitly unstable. The exercise found three defects nothing else could: `tinker-pdf-font` **excluded the CMap registry its own `build.rs` asserts is present**, so the published crate would have built for nobody, four crates into an irreversible upload; every `[workspace.dependencies]` entry needed a `version` beside its `path` or `cargo publish` refuses the whole workspace; and the `.csproj` named a `runtimes/` directory that had never existed, so `dotnet pack` was quietly producing a **managed-only** package that restores, compiles and throws `DllNotFoundException` on first use. What is still owed is a **local registry**: `cargo publish --dry-run` resolves against the live index, so the eight crates above the leaves cannot be proved without publishing, and neither can the plan's own choice to build the language packages against the *published* facade. And the whole CI matrix — every Linux and macOS leg, and the one-tag claim — is written and **unobserved**, because no tag has run it. | 26 |
| **Forms: calculations** | ~~`/AA` scripts are not run~~ **BUILT — option A, against gap 27's own recommendation of option B.** That recommendation's deciding argument is that a half-implementation is worse than none *unless the writes are transactional*; PRE-E built exactly that primitive first, so the argument was answered rather than ignored. Option B's reader landed first and independently green — `/AA`, `/CO`, `/Names /JavaScript` and the catalog's `/AA`, as source text — then the interpreter on top of it. **What is honestly still absent**: keystroke and validate actions are surfaced and never run; document-level scripts are surfaced and never run; nothing recalculates automatically, because when a calculation runs is a host's policy; and format actions produce a display string that deliberately never becomes `/V`. What is present is bounded on purpose — no `eval`, no loops beyond a step budget, no `app.*` beyond inert stubs whose result can never become a field value | 27 |
| **XPS** (30) | **built** — the second of the three formats gap 28 decided | An `.xps` or `.oxps` opens as a `Document`, by the same synthesis CBZ uses and for the same reason. **It also closed a live defect**: gap 29 taught `Document::open` to sniff `PK\x03\x04`, and one signature covers CBZ, XPS, EPUB, ODF, OOXML and every JAR — so before gap 30 a real XPS opened as a *comic*, one page at the size of whichever raster resource it happened to hold, with the markup, the text, the fonts and the page size discarded and **no warning at all**. ECMA-388 E.3's three-step test is what tells them apart now. Ours all the way down: an XML pull parser in an eighth leaf crate (`<!DOCTYPE` refused **by its own name** before one byte after it is read, which is the whole defence against entity expansion), OPC part naming and relationships, 11.2.3's abbreviated geometry, 15's brushes, 12.1.3's `Indices`, and 9.1.7.3's ODTTF de-obfuscation. The writer grew the half PDF needs for it: `/ExtGState`, `/Shading`, `/Pattern`, and **Type0/CIDFontType2 with `/Identity-H`**, without which a glyph cannot be addressed by index at all — which is how XPS addresses every glyph. Images **pass through** as gap 29's do: a page whose decoded raster would be 17.2 MB costs 0.2 MB above baseline to synthesise, measured rather than argued. **Eight genuine packages from two Microsoft producers are committed**, bought in milestone 1 *before* any reader existed so gap 29's "no real file was ever opened" could not repeat, and all eight now render with nothing owed. What is not read: `VisualBrush`, remote resource dictionary parts, TIFF and JPEG XR (refused by name), `IsSideways`, odd `BidiLevel`, `OpacityMask`. **No non-Windows producer** has been found, so the corpus is one vendor's idea of the format |
| **EPUB** (31) | **built** — the third and last of the three formats gap 28 decided, and the largest | An `.epub` opens as a `Document` whose pages are its **laid-out spine**, by the same synthesis CBZ and XPS use. **It closed the last of the one-signature defect**: Frankenstein — 31 content documents, 3 stylesheets — opened as *one page* at the size of its auto-generated cover, with no warning at all, and a book with no image was refused as `NoImages`. An OCF container is now recognised by `META-INF/container.xml` alone, which is the one file §4.2.6.3 makes mandatory. Ours all the way down again, and two more leaf crates for it: **`tinker-pdf-css`** is `css-syntax-3`'s tokenizer with its normative error recovery, `selectors-4` matching and specificity, `css-cascade-5`'s whole sorting order and a `@media` evaluator; **`tinker-pdf-layout`** is CSS 2.2's box model, §8.3.1's collapsing margins, §9.4's formatting contexts, §9.5's nine float rules, §17's table model with §17.2.1's anonymous objects, `css-flexbox-1`, §13.3's fragmentation, and **UAX #14 line breaking over vendored Unicode 17.0.0 data** — 19 338 of 19 338 of the UCD's own pair table. `tinker-pdf-xml` grew a doctype mode, because pandoc writes `<!DOCTYPE html>` on every content document and the parser refused it; the internal subset, where all four of gap 30's bombs live, stays refused by name. The writer grew the half PDF needs for a book: **link annotations and a document outline**, which `DocumentBuilder` had none of. **A book's page count is a function of `OpenOptions`, not of the file** — stated on the type rather than left to be found. How a partial CSS implementation is made *visible* is the design: a property parsed with no consumer **does not compile**, every chapter's characters are conserved exactly, and what is unimplemented is reported by name and counted by elements reached. Six books from two real producers are committed and twenty more are fetched; a headless Chrome is a fifth oracle, because comparing a CSS implementation against a partial one is comparing it against nothing. What is not read: SVG content documents, `min-width`/`max-width`/`min-height`/`max-height`, `vertical-align`, `gap`, multi-column, and `position` other than static. **No fixed-layout book from a real producer and no real producer's font through the `@font-face` path** — both recorded as owed |
| **Tinker integration** | Tinker still runs on MuPDF and does not depend on this engine at all — but it is no longer waiting on a decision. **All three owner decisions are answered and dated**, in plan 15 where the options used to be: EPUB, XPS and CBZ are to be **built natively here** (a fourth option gap 28 did not contain, reaching the licensing outcome dropping them was recommended for, and costing this engine its PDF-only identity — CBZ S, XPS L, EPUB XL+, three gap plans to follow); form calculations are option A and already built; and Tinker's licence becomes **MIT OR Apache-2.0**, with the caveat that relicensing existing Tinker code needs its contributors' agreement, which is outside this repository. The **deletion checklist is re-verified** against Tinker at `f33ce8a` — all twenty-two items still present, nothing moved, and five traces the checklist does *not* name found instead, one of which breaks the released source archive because `git archive` does not follow the submodule Tinker has since gained. The `FontProvider` blocker is closed on this side and planned on Tinker's as milestone 15.1a: faces come from a bundled set first and the system directories second, because Tinker's own golden strategy names bundled fonts as its mitigation for cross-platform flake. What is still owed before the swap is an oracle pass and a `--fonts` corpus bar | 28 |

## Where Tinker integration stands

Tinker's engine layer is eleven functions wide — open, page count, metadata,
permissions, geometry, encryption info, render, text, search, outline — and
every one is covered, several better than MuPDF covered them: real permission
flags, owner-versus-user, a pinned A4-at-150-dpi size, destinations as an enum,
`Send + Sync` reads.

~~Two things still block the swap, and neither is a missing feature.~~ **The
three owner decisions are answered and dated in plan 15; what still blocks the
swap is evidence and one piece of Tinker-side work.**

**The corpus has run, and it settles half of the precondition.** 4 525 files,
4 484 rendering every page, zero crashes — so "validated on four self-authored
fixtures and eight files from one laptop" is no longer the state. But gap 23
counts a bitmap that came back, not a bitmap that is right: Checkpoint B asks
for render **parity** at 95 %, and there is no oracle comparison anywhere in
that run. What is owed is an `oracle-diff` pass over a corpus subset and a
`--fonts` bar recorded once Tinker has a provider, in that order.

**This engine will read EPUB, XPS and CBZ.** ~~This engine reads PDF and will
not read those.~~ *Decided 16 August 2026.* MuPDF opens all three, which is
what `Doc::Other` exists for; rather than drop them, shell out or keep MuPDF
for those formats alone, they are being **built natively here**. That keeps
the licensing outcome — MuPDF leaves Tinker's tree entirely — and costs the
identity: this is no longer a PDF-only engine, and every place that said it
always would be has been amended. Sized with the costs on the table: **CBZ S,
XPS L, EPUB XL+**, the last being a layout engine rather than a renderer and
larger on its own than the whole gap programme just completed. Three gap plans
follow, one per format. See 28.

**All three are built.**
29 closed in August 2026: a `.cbz` opens, pages in
natural order, at native resolution, with everything this build cannot read
refused **by name**. `tinker-pdf-zip` is the seventh leaf and gap 30 used
it, because an XPS document is an OPC package and an OPC package is a ZIP —
that was the second reason it is its own crate. 30
closed on 19 August 2026 and 31 on 22 August, the last
adding two leaves of its own — `tinker-pdf-css` and `tinker-pdf-layout`, the
ninth and tenth — and taking the workspace's own answer to *"what is a leaf"*
from a list to a definition. **The deletion checklist can run**: all three
formats are answered.

**Tinker's licence becomes MIT OR Apache-2.0**, matching the engine, with the
caveat that relicensing existing Tinker code needs its contributors'
agreement — a step outside this repository. The iOS App Store blocker was
about the dependency rather than about Tinker's own terms, so it clears
either way.

**The `FontProvider` handover is the one blocker with code still owed**, and
all of it is Tinker's. The seam here is built, tested and projected across
every binding, and it is measurable: `testdata/simple-text.pdf` renders 0
inked pixels with `UnreadableFont` on all three pages and no provider, and
4 192 on page one at 150 dpi with three real faces. Where Tinker's faces come
from is now decided — a bundled set first, system directories second — and
planned as plan 15's milestone 15.1a, which gates the golden re-baseline.

## The honest summary

The engine reads PDFs properly and now renders them substantially correctly:
the silent-wrongness class of defect — rotation, patterns, soft masks, tint
transforms, stroke parameters, text render modes — has been found and fixed,
each with a test that would have caught it. It writes valid files, compresses
them, and no longer claims encryption it did not apply.

~~What it still has not done is meet a real corpus.~~ It has now met one —
4 525 documents, 4 484 rendering every page, not one crash — so "everything
above is true of inputs this repository wrote" is no longer the caveat. The
caveat that replaces it is narrower and harder: **nothing has compared a page
this engine drew against a page anything else drew.** The corpus measures
whether a bitmap came back. Whether it is the *right* bitmap is
`oracle-diff`'s question, and it is the next thing worth doing, ahead of any
new feature.
